// import {
//   setspeaking
// } from "@/main";

// let curr: number = 0;
// let route: string = "";

export default async function next() {
  // if (!dm) return;
  // setspeaking(true);

  // clear();

  // curr++;

  // setspeaking(false);
}